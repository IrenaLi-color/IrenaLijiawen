# 李佳文个人网站

这是一个可直接部署到 GitHub Pages 的静态个人主页。

## 部署

1. 新建 GitHub 仓库，仓库名使用 `<你的GitHub用户名>.github.io`。
2. 将本目录中的文件推送到仓库 `main` 分支根目录。
3. 在浏览器访问 `https://<你的GitHub用户名>.github.io/`。

## 文件

- `index.html`：页面结构
- `styles.css`：视觉样式与响应式布局
- `script.js`：照片入口、联系弹窗、详情弹窗交互
- `assets/images/`：从素材文档提取的图片资源
